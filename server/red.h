/*
	Deteccion de errores y direccionamiento
*/

/*
bitset<8> foo (std::string("10110011"));

  cout << foo << " has ";
  cout << foo.count() << " ones and ";
  cout << (foo.size()-foo.count()) << " zeros.\n";
  */
  
  
  
  /*bitset<8> foo; 
	foo.set(3,1);
	cout << "foo: "<< foo << '\n';
	unsigned long i = foo.to_ulong(); 
	char c = static_cast<unsigned char>( i );
	//cout << "Valor de prueba    " << hex << static_cast<int>(prueba) << endl;
	cout << "foo: " << hex << static_cast<int>(c) << '\n';*/
